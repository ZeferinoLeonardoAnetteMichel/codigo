[project]
name = "codigo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flet>=0.85.1",
]
[project.scripts]
app="main:main"
[tool.uv]
package=true
[tool.setuptools.package-dir]
"" = "src"
